on:
  push:
  workflow_dispatch:
  
jobs:
  test-mac:
    uses: ./.github/workflows/test.yml
    with: 
      os: macos-latest
  test-ubuntu:
      uses: ./.github/workflows/test.yml
      with: 
        os: ubuntu-latest
  test-windows:
      uses: ./.github/workflows/test.yml
      with: 
        os: windows-latest
